% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path_collapse.R
\name{path_collapse}
\alias{path_collapse}
\title{Collapse consecutive identical locations in paths}
\usage{
path_collapse(paths, stap = NULL, label_stap_id = FALSE)
}
\arguments{
\item{paths}{A path data frame containing \code{stap_id}, \code{ind}, \code{lat}, and
\code{lon}, and optionally \code{j}, \code{chain}, \code{start}, and \code{end}.}

\item{stap}{A stationary-period data frame containing \code{stap_id}, \code{start},
and \code{end}. If \code{NULL}, these columns are taken from \code{paths}.}

\item{label_stap_id}{logical; if \code{TRUE}, replace \code{stap_id} in the output
with a character label of the form \verb{"\{stap_id_start\}->\{stap_id_end\}}.
The numeric \code{stap_id_start} and \code{stap_id_end} columns are always kept.}
}
\value{
A data frame with one row per collapsed stay and columns
\code{stap_id}, \code{stap_id_start}, \code{stap_id_end}, \code{ind}, \code{lat}, \code{lon}, \code{start},
\code{end}, \code{known}, \code{j}, and \code{chain}.
}
\description{
Summarise each realised path by merging consecutive stationary periods that
were assigned to the same grid cell. Paths are identified by \code{j} and, when
present, \code{chain}.
}
\seealso{
Other path:
\code{\link[=path_summary]{path_summary()}}
}
\concept{path}
