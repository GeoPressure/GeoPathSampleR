% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path_summary.R
\name{path_summary}
\alias{path_summary}
\title{Summarise an ensemble of paths}
\usage{
path_summary(
  paths,
  stap = NULL,
  by = c("stap", "consensus_stay"),
  position = c("mean", "median", "mode"),
  move_threshold = 0.5
)
}
\arguments{
\item{paths}{A path data frame containing \code{stap_id}, \code{ind}, \code{lat}, and
\code{lon}, and optionally \code{j}, \code{chain}, \code{start}, and \code{end}.}

\item{stap}{A stationary-period data frame containing \code{stap_id}, \code{start},
and \code{end}. If \code{NULL}, these columns are taken from \code{paths}.}

\item{by}{Summary resolution: \code{"stap"} returns one row per stationary
period; \code{"consensus_stay"} returns one row per consensus stay.}

\item{position}{Position summary: \code{"mean"}, \code{"median"}, or \code{"mode"}.
The mode selects the most frequent grid cell and therefore retains a
valid \code{ind}; the mean and median return \code{NA} for \code{ind}.}

\item{move_threshold}{Probability above which a boundary is classified as a
move when \code{by = "consensus_stay"}.}
}
\value{
A data frame with one row per requested summary unit and columns
\code{stap_id}, \code{stap_id_start}, \code{stap_id_end}, \code{ind}, \code{lat}, \code{lon}, \code{start},
\code{end}, and \code{n_path}. Consensus summaries also contain movement
probabilities at the start and end boundaries.
}
\description{
Compute either one posterior location per stationary period or one path
using the consensus stay/move structure across the ensemble. The function
accepts both \code{\link[=sampling_path]{sampling_path()}} output and simulation paths with \code{j} but no
\code{chain} column.
}
\seealso{
Other path:
\code{\link[=path_collapse]{path_collapse()}}
}
\concept{path}
