% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_path.R
\name{sampling_path_default_movement}
\alias{sampling_path_default_movement}
\title{Default movement model for path sampling}
\usage{
sampling_path_default_movement()
}
\value{
A movement list with fields \code{method}, \code{shape}, \code{scale},
\code{low_speed_fix}, \code{zero_speed_ratio}, \code{move_stay_parameters}, and
\code{move_stay}.
}
\description{
Return the calibrated movement model used by default in \code{\link[=sampling_path]{sampling_path()}}.
Call \code{sampling_path_default_movement()} to inspect its values, plot it with
\code{\link[=plot_movement]{plot_movement()}}, or modify some fields before passing it to
\code{sampling_path(movement = )}.
}
\details{
The movement model defines the daily transition between consecutive
stationary periods. It is a list with two parts.

\strong{Ground-speed kernel.} The fields \code{method}, \code{shape}, \code{scale},
\code{low_speed_fix} and \code{zero_speed_ratio} are passed to
\code{\link[GeoPressureR:speed2prob]{GeoPressureR::speed2prob()}} and follow the convention of
\code{\link[GeoPressureR:graph_set_movement]{GeoPressureR::graph_set_movement()}}. Speed is the great-circle displacement
between two consecutive periods divided by 24 h, in km/h. With
\code{method = "gamma"}, the speed density is a Gamma distribution with shape
\code{shape} and scale \code{scale} (km/h). The probability of moving to a given cell
is this density divided by the distance and multiplied by \code{cos(latitude)},
which accounts for the number and area of cells at that distance. The hard
movement radius is set separately by \code{sampling_path(thr_gs = )}.

\strong{Residence-dependent departure.} \code{move_stay} is a function of the
residence \code{t}, the number of consecutive transitions spent at the current
location (\code{0} for the first transition after arrival), returning the
probability of departing at the next transition. The default is the
exponential decline to a plateau
\code{p_inf + (p_0 - p_inf) * exp(-t / tau)}. \code{move_stay_parameters} stores
\code{p_0}, \code{p_inf} and \code{tau} so that \code{\link[=plot_movement]{plot_movement()}} can annotate them; it is
not used by the sampler, so update \code{move_stay} when changing them.

The optional field \code{approx_eps} (default \code{1e-6}) drops movement-kernel
weights below this value from the precomputed neighbourhood.

The default values were calibrated on multi-sensor reference trajectories
of land birds (Gamma shape \code{1.26} and scale \code{10.3} km/h; \code{p_0 = 0.607},
\code{p_inf = 0.137}, \code{tau = 0.932}). They should not be assumed to transfer to
other taxa.
}
\examples{
movement <- sampling_path_default_movement()
str(movement)
plot_movement(movement)
}
\seealso{
Other sampling_path:
\code{\link[=plot_movement]{plot_movement()}},
\code{\link[=sampling_path]{sampling_path()}},
\code{\link[=sampling_path_diagnostic]{sampling_path_diagnostic()}}
}
\concept{sampling_path}
